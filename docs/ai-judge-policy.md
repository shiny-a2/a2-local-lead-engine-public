# AI Judge Policy

AI Judge is optional and only runs when judge flags, global AI flag, OpenAI key, and judge model are configured. It must return JSON only and cannot override Rule Judge blockers.

AI Judge provides scores and rewrite guidance only. It must not rewrite the email body or approve sending.
