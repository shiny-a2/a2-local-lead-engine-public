# URL Probe Safety Policy

URL probes are lightweight: HEAD first, limited GET only when necessary, no crawling, no browser
automation, no JavaScript rendering, and no form submission. Timeout and max-byte limits are
enforced.

