# Source Category Mapping

Mappings live in `app/config/source_category_map.yaml`.

Initial placeholders:

- `barber`
- `beauty_salon`
- `cleaning_service`

These source taxonomies may need manual correction after real sample review. The mapping is
configuration-driven so connector code does not deeply hardcode source-specific categories.

