# Manual Follow-up Operations Policy

Follow-up plans may only be `manual_only`, `not_allowed`, or `future_phase`.

Phase 13 never schedules or sends follow-ups. `DONE_MANUALLY` means a human performed an action outside the system.
