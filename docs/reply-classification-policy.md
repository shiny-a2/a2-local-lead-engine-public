# Reply Classification Policy

Rule-based classification is the default. Categories include positive interest, price request, details request, call request, not interested, already has website, wrong contact, unsubscribe request, auto reply, out of office, bounce-like, and unknown.

AI classification is optional and may classify only. It must never write reply text.
