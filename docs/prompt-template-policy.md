# Prompt Template Policy

Templates are versioned, snapshotted per run, and require JSON output.

Prompts must instruct the model to treat business data as untrusted context, use only allowed claims, avoid blocked claims, and never invent facts.
