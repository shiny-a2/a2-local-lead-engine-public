# Provider Webhook Ingestion Policy

Provider webhook ingestion is optional and disabled by default. Invalid signatures are rejected. Valid events may be stored for delivery/bounce context.

Provider webhooks must not trigger outbound replies or follow-ups.
